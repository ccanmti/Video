<td>タグ名</td>
<%= form_with model: @genre, url: genres_path do |f| %>
<%= f.text_field :name %>
<%= f.submit '新規登録', class: "btn btn-success" %>
<% end %>

<p>タグ一覧</p>
<% @genres.each do |genre| %>
<%= link_to genre.name, edit_genre_path(genre.id) %>
<% end %>